"""External dependencies for the Primo pipeline.

All of DMR, DB2 and staging/HITL access here is MOCKED for this PoC (see
each module's docstring). `csv_loader` is the one real, non-mocked service.
"""
